kind: pipeline
name: default

steps:
  - name: build
    image: node:current-alpine
    commands:
      - npm config set ${NPMRC}
      - yarn
      - cd packages/uikit-react
      - yarn build
      - yarn publish

