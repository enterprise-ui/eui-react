kind: pipeline
name: default

steps:
  - name: build
    image: node:current-alpine
    environment:
      NPMRC:
        from_secret: NPMRC    
    commands:   
      - npm config set registry $NPMREGISTRY
      - yarn
      - cd packages/uikit-react
      - yarn build
      - npm publish

